# vim: ts=2:sw=2:softtabstop=2
machine:
  python:
    version: 3.6.0 

general:
  artifacts:
    - artifacts/

dependencies:
  pre:
    - make deps_circle
  post:
    - make download_curves
  cache_directories:
    - "data/vex/cim/"
    - "data/vex/mini-cim/"
    - "data/vex/775pro/"
    - "data/vex/bag/"

test:
  override:
    - make test
