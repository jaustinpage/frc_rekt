machine:
  python:
    version: 3.6.0 

dependencies:
  pre:
    - make deps_circle
  post:
    - make download_curves
  cache_directories:
    - "vex_data/cim/"
    - "vex_data/mini-cim/"
    - "vex_data/775pro/"
    - "vex_data/bag/"

test:
  override:
    - make test
